import React from 'react'

const test = (props) => {
  
  return (
    <div><center>
      <h1>Name:{props.name} and Age:{props.age} and City:{props.city}</h1>
      </center></div>
  )
}

export default test